# Local: để trống — request tới `/api/...` qua Vite proxy (vite.config.ts).
# Production (Vercel): origin backend, không có dấu / cuối, ví dụ:
# VITE_API_URL=https://ai-no-code-backend.onrender.com
VITE_API_URL=
